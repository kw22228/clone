import DarkSwitch from '../../animation/darkSwitch.json';

const lottieOptions = {
    animationData: DarkSwitch,
    loop: false,
    autoplay: false,
    rendererSettings: {
        className: 'add-class', // svg에 적용
        preserveAspectRatio: 'xMidYMid slice',
    },
};
const DarkMode = () => {
    return;
};
export default DarkMode;
